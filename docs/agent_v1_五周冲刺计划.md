# Agent v1 五周冲刺计划（2026-09-24 ~ 2026-10-28）

> 本文是 v1 的**施工图**。它是"真要用的项目"，按 `docs/项目开发协议_v1.md` 全套走。
> 起草：2026-09-23（军训最后一天）；**同日按学员更正重排目标函数**（见 §0）。
> 依据：学员确认的节奏（每格 1.5–2 小时）+ 本机环境实测。
> **进度权威快照仍只看 `memory/02_当前状态.md`**，本文只定义"做什么、验收什么、什么时候砍"。

---

## 0. 一句话目标（2026-09-23 学员更正）

> **这个项目的目的是两个：① 学习、理解新技术，提高能力和基础；② 作为简历，为实习创造优势。**
> **它不是为了让学员有个工具能用，而是为了理解并掌握 agent 开发。**

→ **目标函数因此改写**（这是本次最重要的更正）：

| | 旧口径（已作废） | **新口径** |
|---|---|---|
| 优化对象 | 工具的**可用性与鲁棒性** | **理解深度 + 可展示的证据** |
| 代码行数 | 1200–1800 行是目标 | 行数只是**副产品**，不是指标 |
| 验收 | "连续 5 天没有一天因为它出错而改回手做" | **每个模块你能讲出"为什么必须有它、没有它会怎样"**，且有自己的证据 |
| 砍功能的理由 | "功能少 = 你敢用" | "这个功能**教不到东西**，才砍" |

**"天天用"降级为加分项，不再是验收线**：仍然要能跑通、要真的用它干过活（不然是空的），
但**不追求"每天都靠得住"**——那会吃掉大量时间在边缘 case 上，而那些 case 教不到 agent 原理。

### v1 的验收标准（三条，缺一不可）

1. **结构能画**：不看资料，凭记忆画出 agent 的数据流与模块结构（你有这个强项——L17 复习课第一版就画到六到七成）。
2. **机制能讲**：每个模块用"12 岁能听懂"的话讲一遍，并指出**它解决什么、不解决什么**。
3. **坑能复现**：能**主动造出**下面这些故障并解释你怎么发现的、怎么修的：
   - 工具调用死循环（用 `maxTurns` / `maxUnboundToolCalls` 兜住）
   - `reasoning_content` 漏传 → 400，以及为什么报错信息不指向原因
   - 后台 asyncio 任务静默消失（Python 3.12 弱引用）
   - 429 与指数退避（含"哪些错误**不该**重试"）
   - 上下文被压缩前后，会话内容发生了什么变化

> **判据一句话：能不能拿它去面试讲 20 分钟，且面试官追问"为什么这么设计"你能答。**

---

## 1. 砍掉清单（**判据已更正：不是"用不上"，而是"教不到东西"**）

| 砍掉 | 理由（2026-09-23 更正后） |
|---|---|
| TUI（pi-tui） | **纯工程体力活，零 agent 原理** |
| 沙箱 / Docker / 权限系统 | 运维话题，不是 agent 开发的核心 |
| RPC 模式 / SDK 嵌入 | 协议包装，学不到循环本身 |
| Python 移植版（pi-coding-agent PyPI） | 和 v1 是同一个东西，重复 |
| DSML 解析器 | **已核实是自托管问题**。你走官方 API，拿到的是标准 `tool_calls`；本机 dsh 的 DeepSeek 适配器源码里也没有一行 DSML 解析 |
| 子 Agent / Skills / 扩展**加载机制** | 要看懂"机制"，**不需要自己实现一套**——见 §9「读什么、不读什么」 |
| MCP **适配器**（把 MCP 工具转成 `registerTool`） | 机械转换。**但 MCP 协议本身要看懂**（它是什么、解决什么问题、和函数调用的区别）——这是面试高频题，2 格以内 |

> ⚠️ **更正提示**：上一版写"功能少 = 你敢用"，那是按**旧口径（工具好不好用）**推出来的。
> 按新口径（理解 + 简历），砍功能的唯一理由是**学习密度低**，不是"用不到"。
> 反过来，**"生产级"里有一半恰恰是最该学的**（限流退避、成本核算、上下文工程、故障观测）——
> 这些是 12 周培训班不教、只有从零写一遍才会懂的东西。
> **简历的含金量来自"把难的地方钻透"，不是来自功能清单的长度。**

---

## 2. 五周日历（15 格）

每格结构固定：**10 分钟概念问答 → 60–80 分钟动手 → 5–10 分钟验收**。
每周结束时必须能回答"这周我获得了什么能力"——答不出来就是这周白过了。

| 周 | 日期 | 格 | 内容 | 交付物 | **这周结束时你能讲清什么** |
|---|---|---|---|---|---|
| **W1** | 09/24–09/30 | 1 | `async` / `await` / 事件循环（用你已经会的"6 站因果链"接上去） | `test_async.py` 能并发发起两个请求 | 为什么等待 I/O 时程序必须能去干别的；async 到底解决了什么 |
| | | 2 | 异常传播 + 任务注册表（Python 3.12 任务会被 GC 静默回收，必须强引用持有） | `test_tasks.py` 故意让后台任务抛异常，**必须能看到它** | 为什么"没报错"不等于"没问题"；弱引用与任务生命周期 |
| | | 3 | **事件系统骨架定型**：`emit()` 先归约 state，再按注册顺序 `await` listener | `emit` 顺序定死 + 一条日志能证明顺序 | 为什么顺序反了，第 3 周的持久化就会存进旧数据 |
| **W2** | 10/01–10/07 | 4 | Pydantic 定义 `AgentTool` 与参数 schema | 4 个工具的 schema 能打印出来 | 为什么工具要给模型一份"结构化说明书"，而不是自然语言描述 |
| | | 5 | `tool_calls` 协议 + Agent Loop 的 while 循环 | 循环能识别"模型要调工具"并停下 | agent 的本质为什么就是一个 while 循环 |
| | | 6 | **第一个真请求跑通**：流式输出 + 一轮工具调用闭环 | `python agent.py --once "读一下 README 第一行"` 成功 | 流式输出为什么能显著改善体感与成本 |
| **W3** | 10/08–10/14 | 7 | 4 个工具实现（含路径安全、`edit` 用 search-and-replace） | 4 个工具各自有验收样例 | 为什么 `edit` 要用"查找替换"而不是整文件重写 |
| | | 8 | JSONL `append-only` 会话存档 + `reasoning_content` 全程回传 | 会话能存、能 `--continue` 恢复，**第二次请求不报 400** | append-only 为什么比"改历史"可靠；`reasoning_content` 为什么必须回传 |
| | | 9 | 命令行接口 + 状态恢复 | `--continue` 能接着上次聊 | 会话状态到底由哪些字段构成 |
| **W4** | 10/15–10/21 | 10 | 上下文压缩（`contextTokens > contextWindow − 16384` 触发，摘要作为 `CompactionEntry` 追加） | 压缩前后会话仍可用，历史条数不减 | 上下文为什么会"腐化"；摘要为什么要追加而不是删改 |
| | | 11 | token 账（缓存命中/未命中/输出**分开记**）+ 429 退避（≤5 次、带 jitter） | `ledger.jsonl` 有数，且**与 API 后台对得上** | 为什么缓存命中和未命中差几十倍；哪些错误**不该**重试 |
| | | 12 | 熔断闸（`maxTurns=20`、`maxUnboundToolCalls=3`）+ 成本对账 | 故意造一个死循环，**它必须自己停下来** | Token 预算为什么挡不住死循环，必须靠圈数熔断 |
| **W5** | 10/22–10/28 | 13 | **故障复现日**：主动造出 §0 里那五个故障，逐个记录"怎么发现/怎么定位/怎么修" | 5 条故障档案 | 你对这套系统的失效模式了如指掌 |
| | | 14 | **结构复盘**：不看资料画数据流与模块结构图；再对照 Pi 的结构拆解文章找差距 | 结构草图（第 2 版） | 你能把整个 agent 讲成一张图 |
| | | 15 | **收口**：机制复述（费曼式）+ README + 简历段落 | v1 完成，可对外展示 | 这就是你面试时讲的 20 分钟 |

**最低可行版本 = 12 格**（W1 三格 + W2 三格 + W3 三格 + W4 两格 + W5 一格）。

---

## 3. 三个检查点（到点不达标就砍，不延期）

| 检查点 | 判据 | 不达标怎么办 |
|---|---|---|
| **W1 末（09/30）** | `emit()` 顺序定死，且流式打印能跑 | W2 降级为**非流式**，先跑通循环 |
| **W2 末（10/07）** | **真请求能完成一轮工具调用闭环** | **这是生死线**：不达标 → 接受"五周只到 W2 的水平"，但 **W3–W4 的坑（`reasoning_content`、限流退避、上下文压缩）改为手写复现练习**，保证该学的机制一个不落 |
| **W3 末（10/14）** | 会话能存、能恢复，且会话第二次继续**不报 400** | 砍压缩（W4 只留熔断 + 重试），但 §0 第 3 条的五条故障档案必须做满 |

> **注意检查点的性质变了**：新口径下，**"没做完"不等于"没学到"**。
> 所以不达标的处理不是"砍掉不做了"，而是**降低实现方式（照文档手写一小段复现），保住机制层面的理解**。

> **W1 末要现场数一次真账**（不许估）：实际用了几格、每格多久、卡点集中在哪。
> 用它替换本文后面的**所有**进度推算——**编制周计划时不做第二次估算**。这条同时是对教员的约束：
> 凡"还要几周/几格"的数字，**要么来自这个实测本，要么不说**。

---

## 4. 五条纪律

1. **v1 跑通之前，不许读 Pi / dsh 的源码**（读结构分析可以，见 §9）。**Why（新口径）：** 你的目标是"理解并掌握"，而
   **抄一遍源码 = 能力错觉的最高级形式**——代码跑起来了，但每一行为什么这么写你都说不出。
   跑通之后再对照读，那时读的是"我当时为什么没想到"，学习效率最高。
2. **推理强度默认 `off`**（`thinking:{"type":"disabled"}`），需要时开 `low`。默认 `high` 会让每次工具调用前产生推理 token，而它**按输出价计费**。
3. **每格结束必须独立可验证**。不接受"这格写了一半"——写不完就当场缩小这一格的目标。
4. **每格先建 venv**：`python -m venv .venv` → 激活 → `python -m pip install httpx pydantic`。
   装完**必须** `Get-Command python` 确认指向 `.venv`（你已经栽过"改了东西 ≠ 它进了该去的地方"）。
5. **遇到报错，原样贴回**（连代码一起）——这是你已经定好的机制。
6. **【2026-09-23 学员否定旧纪律】不设"每格只能学一个新概念"这类额度。**
   **旧条作废**：那是我把学员 09-21 立的"教员一条消息只推一个概念"（约束的是**我的输出方式**）
   挪用成了"学员每格只能学一个新概念"（约束你的学习速度）——**这是两回事，后者会变成教员拖延的借口**。
   **改成**：**按应有流程接触新概念——哪个概念挡在交付物前面，就把它学到够用。**
   只保留一条兜底（**不是额度**）：**同一个点卡超过 20 分钟**，先写一句"卡在第几步、试过什么"再继续。
   **Why:** 五周冲刺的目标函数是理解 + 交付，人为限速直接损害它。
   **How to apply:** 每格开头可以直接认领"这格要碰 N 个新概念"；教员不得以"额度"为由推迟或简化。

---

## 5. 三个必须提前知道的坑（核实过，不是想出来的）

### 坑 1：`reasoning_content` 必须全程回传（**v1 最致命的一个**）

官方 Thinking Mode 文档原文：只要请求带 `tools` 参数，**所有历史轮次的 `reasoning_content` 都必须完整传回 API，否则直接返回 400**——即使某一轮模型没有调用工具也一样。

→ 影响：**W3 的会话存档如果不存 `reasoning_content`，会话第二次继续就永久 400**，而且报错信息不会告诉你原因。
→ 做法：存档时 assistant 消息必须同时写 `content` + `reasoning_content` + `tool_calls` 三个字段。

### 坑 2：Python 3.12 的 asyncio 任务会被静默回收

事件循环对 `asyncio.create_task()` 创建的任务**只持有弱引用**。没有强引用的任务可能在 `await` I/O 时被垃圾回收，**不抛任何异常**——表现为"Agent 跑着跑着就不动了"。

→ 做法：维护一个全局任务注册表（`set()`），创建时 `add`，`done_callback` 里 `discard`；后台任务必须保留返回值并挂异常回调。

### 坑 3：`emit()` 顺序必须在 W1 定死

**先归约内部 state（含 assistant message 的 `tool_calls` 提取），再按注册顺序 `await` listener。**

→ 如果顺序反了，listener 读到的是**更新前的 state**；等到 W3 加持久化时，存进去的就是旧数据——**这个顺序 W1 不定，W3 必返工**。

---

## 6. W1 施工单（09/24–09/30，三格）

**W1 结束你应当能回答**：为什么 `emit()` 必须先更新 state 再叫 listener？——用自己的话，不用术语。

> **本格不设概念额度**（见 §4 第 6 条）：哪个概念挡在交付物前面就学哪个，学到够用为止。

### 第 1 格：`async` / `await` / 事件循环
- 入口不是"讲 async 语法"，而是**接上你已经知道的因果链**：特别课 A 里的"跳转"，在 I/O 场景下为什么会阻塞整个程序。
- 产出 `test_async.py`：**并发**发起两个请求，总耗时明显小于串行。
- 验收：你能说出"等待的时候程序去干什么了"。
- 这一格要碰到的东西（按需展开，不漏不赶）：`async def` / `await` / 事件循环 / `httpx` 基本调用 / 从 `requests` 换到 `httpx` 的差异。

### 第 2 格：异常传播 + 任务注册表
- 产出 `test_tasks.py`：一个后台任务故意抛异常。
- 验收：**你必须能看到它**。如果你看到的是"什么都没发生"，这一格就没过。
- 这一格直接对应坑 2。

### 第 3 格：事件系统骨架 + `emit` 顺序定型
- 产出 `agent.py` 的 `emit()` 函数 + 两个 listener（一个打印 state，一个记日志）。
- 验收：日志能证明**两个 listener 看到的是同一份已更新的 state**。
- 这一格直接对应坑 3，**它是 W3 不返工的前提**。

---

## 7. 与环境相关的三条实测结论（2026-09-23）

1. **本机只有一个 Python**：Store 版 **3.12.10**（`python` 指向它）。
2. **当前 `pip list` 里只有 `requests 2.34.2`**——`httpx`、`pydantic` **都没装**。第一格之前先把 venv 建起来。
3. 装库铁律不变：**`python -m pip install 包名`**（不是 `pip install`）。

---

## 8. 术语对照（本文用到的，首次出现给中文全名）

> ⚠️ **这不是前置学习清单。** 下面这些概念**不要求提前学完**——它们会在各自那一格挡在交付物前面时被学到（§4 第 6 条）。本表只用来**认脸**：见到缩写知道它属于哪一站。

| 缩写 | 中文全名 | 会在哪一格出现 | 在地图哪一站 |
|---|---|---|---|
| API Key | 接口密钥 | W1 | B 课：服务器怎么知道"是你" |
| `async` / `await` | 协程与等待 | W1 | 特别课 A：程序等 I/O 时卡在哪 |
| httpx | 支持异步的 HTTP 客户端库 | W1 | L17：装第三方库 + 特别课 B 全链路 |
| Pydantic | 数据校验库（用类型声明定义 schema） | W2 | 新 |
| `tool_calls` | 工具调用（模型返回的结构化请求） | W2 | 新 |
| SSE | 服务器推送事件（流式输出用的传输方式） | W2 | B 课：HTTP 返回结果那一站 |
| JSONL | 每行一个 JSON 的文本文件 | W3 | B 课：序列化（内存 ↔ 硬盘） |
| `reasoning_content` | 思维链字段（思考模式下模型先输出的推理） | W3 | 新 |
| DSL | 领域专用语言 | — | — |
| MCP | 模型上下文协议 | 只读不写（§9、§10） | 新 |
| token | 词元（模型计费和计量的最小单位） | W4 | 新 |
| TUI | 终端图形界面 | v1 不做 | — |
| venv | 虚拟环境（给单个项目单独装库的隔离目录） | W1 第 1 格 | L17：装第三方库 |

---

## 9. 读什么、不读什么（**这条直接服务"理解"这个目标**）

**读结构分析（高价值、零代写风险）**——它给你**地图**，不给你代码：

- 《拆解 Pi 的 harness 设计》（`walkinglabs.github.io/learn-harness-engineering/zh/harness-designs/pi/`）——用"指令 / 工具 / 环境 / 状态 / 反馈"五子系统拆 Pi，**和你 L17 画结构图是同一套方法**。
- 同站还有 Claude Code / Codex / DeepSeek Harness 的拆解。**横向对比三家**是简历上的加分内容，也只需要读，不需要写代码。
- 官方三页：Tool Calls、Thinking Mode、Models & Pricing。**这三页要能背出关键结论**。

**不读实现源码**（直到 v1 跑通）：

| 不读 | 理由 |
|---|---|
| Pi 源码（`badlogic/pi-mono`，TypeScript） | 语言不同，且抄一遍 = 能力错觉的最高级形式 |
| **本机 `D:\code\dsh` 的 TypeScript 源码** | **这是最容易踩的坑**——它就在你硬盘上，随手就能读。跑通之后再读 |

> 一句话：**先自己有想法，再看别人怎么想；先跑通，再对照。**

---

## 10. 简历策略（"第二个目的"落地成可执行动作）

**不要罗列功能。** 面试官对"我写了个 agent 支持 10 个工具"无感，对下面这些会追问：

| 准备什么 | 为什么这就是优势 |
|---|---|
| **一份《我踩过的 5 个坑》**（W5 产出） | 只有真写过的人才知道这些失效模式——**这是最硬的能力信号** |
| **一次真实的排错**：会话第二次请求 400 → 定位到 `reasoning_content` 漏传 | 展示"读文档 + 读报错 + 定位根因"的完整链路 |
| **一次主动的设计决策**：为什么 `edit` 用 search-and-replace | 展示你能解释设计取舍，而不是照抄 |
| **一张结构图 + 能脱稿讲 20 分钟** | 大多数候选人的 agent 项目讲不清自己代码的结构 |
| **成本账**：缓存命中/未命中差几十倍，你怎么利用它 | 展示你知道这东西在真实场景里**要花钱** |

**面试必问、必须能答的 6 问**（提前准备）：

1. agent 循环的本质是什么？什么时候该停？
2. 流式输出除了"体感快"，还有什么好处？
3. 工具调用失败了怎么办？模型一直调用不存在的工具怎么办？
4. 上下文满了怎么办？为什么摘要要"追加"而不是"删掉旧消息"？
5. 你怎么控制成本？429 来了怎么办、什么错误**不该**重试？
6. **你和直接用框架（LangChain / 现成 agent）有什么区别？**

**先圈定"不做什么"，避免无限扩散**：
本地单用户 / 只对接 DeepSeek / 不联网搜索 / 不做多人协作 / 不接 MCP（除非最后一格有余量）。

**有余力再加、且只加这一件**：**最小 MCP 客户端**（连本机一个 MCP server，把它的工具转成 `registerTool` 调用）。
——理由：**2–3 格的成本换一个当前热门的简历关键词，且能亲手检验"协议适配"这件事有多机械**。放 W5 之后，不进五周保底范围。
