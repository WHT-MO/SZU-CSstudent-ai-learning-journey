# 💡 灵感池（单文件模板）

> 用法：一条灵感一个块，**每块 ≤5 行**，只写能触发行动的要点。
> 生命周期：待孵化 →（决定开工）→ 把"核心需求"抄进 8 问第 1 条并进入项目模式 → 进行中 → 已完成 / 已放弃。
> 纪律：状态列必须更新，否则池子腐烂；不孵化超过 3 个月的就归档或删。

## 模板

```
### 灵感名称：（一句话概括）
- 源头场景：（什么时候、因为什么痛点想到）
- 目标用户：（我自己 / 室友 / 全班）
- 核心需求：（最核心的一件事，只写 1 条）
- 初步思路：（中文流程，不写代码）
- 技术初判：（AI 做哪几步最爽 / 哪一步可能卡住）
- 状态：（待孵化 / 进行中 / 已完成 / 已放弃）
```

---

<!-- ===== 下方开始放你的灵感 ===== -->

### 灵感名称：token 流水记录
- 源头场景：最近学习花费两亿 token（2026-09-17 核实：API 后台显示 226,948,076）
- 目标用户：我自己
- 核心需求：解决 token 花得不明不白
- 初步思路：先做一个日志本，再添加统计和对比功能
- 技术初判：读取本地存档可能卡住
- 状态：已完成（v1：手工录入 + json 整份重写 + 按日期汇总；v2"自动读会话存档"未做）

> 2026-09-17 立项：本项目由 L13/L14 课承接（原日历写作"记账本"，同日改为"token 流水记录"，口径以本块为准）。
> 第 5 条已隐含里程碑顺序（日志本 → 统计/对比），第 6 条已点名风险点（读存档）。
> 2026-09-17 教员核查：本机会话存档 `C:\Users\luolanxier5\.dsh\sessions\--D-code-dsh--\*\session.v3.jsonl.zstd` 里**确实有"每一次调用"的明细**（`inputTokens` / `outputTokens` / `cacheReadTokens` / `reasoningTokens` / `totalTokens`）；但它经 zstd 压缩，Python 侧当前无解压库 → **自动读取列为 v2**。
> 2026-09-17 口径坑（教训，值得进项目文档）：① 部分会话目录里**同时存在**旧格式 `session.jsonl.zstd` 与新格式 `session.v3.jsonl.zstd`（同一会话两份），全目录求和会**加两遍**；② 旧格式里同一次调用的 usage 被记**两次**（对照证据：同一会话两份文件 632 次/192,666,254 vs 316 次/96,333,127，**正好 2 倍**）。→ 只有**单会话 v3** 的数可与客户端显示对上（本次会话实测 3.6M vs 客户端 3.3M）。**结论：口径错一处，数字差一倍**；多文件求和不可信，用后台 1 周窗口口径或单会话口径。
> 2026-09-17 价目（学员从官方页抄，元/百万 token）：输入·缓存命中 空闲 0.02 / 高峰 0.04｜输入·未命中 空闲 1 / 高峰 2｜输出 空闲 4 / 高峰 8。→ 缓存命中是未命中的 1/50、输出的 1/200；**空闲时段全部单价减半**。本次会话约 3.6M token 的实付 ≈ 0.38 元（空闲）／0.76 元（高峰），其中**输出占 70%、缓存命中占 18%**。
> 2026-09-17 后台实际栏目（结构，不是数值）：**模型名 / API 请求次数 / Tokens（一个数）**——**没有输入/输出分列**，且是**按模型的汇总**，不是逐次明细。→ v1 手工录入只能记到"日期 × 模型"这一粒度。
> 2026-09-17 设计决定（学员）：**整份重写**（理由：预计只有几十条记录）；**输入/输出都要统计**（与后台栏目冲突，待定源）；汇总维度倾向**按日期**（与第 4 条"对比不同模型"需对齐）。
> **2026-09-18 完成（v1）**：产物 `code/L13/jizhang_完整版.py`（+ 学员自写版 `jizhang.py`）、运行存档 `code/L13/记账日志.json`。验收 1–4 全过（落盘可续 / 汇总与手算一致 / 存档结构化可读回 / 数据流一句话含"内存"）。**未做**：自动取数（v2）、金额换算、图表、模型对比视图。课程侧定位已校准：本项目是**语法练习载体**（json / datetime / 字典汇总 / 项目流程），按"能用"收口。
