# 🗺️ 当前状态（进度快照 · 只保留最新）

> 开场必读。只放**当前有效**的进度、下一步、待办；成果细节/画像/台账见 `05_历史明细.md`（按需读）。

## 快照时间
**2026-09-20（周日）**：完成 **L17 总复习 +《我的 Python 路线图》** → **军训 Python 主线 17 课全部收官（L01–L17）**，并关掉最后两个触发器（`sys.stdin` / `pip`）；收尾补了 P5711 入库、切片转正、L13 数据流图、首次周体检。

**2026-09-21（周一）维护格（无课）**：
1. **实测 token 账**（14 会话 / 1676 次调用 = **3.28 亿 tokens**，其中 **99.0% 是每轮重发的上下文**、输出仅 0.6%）→ 立 **token 卫生**纪律（工具输出瘦身 / 输出瘦身 / 一格一存档），已入 `docs/教学机制与协作约定.md` 机制 4。审计由学员走 API 后台，仓库不落脚本。
2. **仓库瘦身**：`memory/03_会话纪要/`、`01_学员档案.md`、`05_历史明细.md` **移出 GitHub（本地保留）**；删 `00_开场词.txt`（口令＝"按协议开始"）与 `code/L17/test_in.txt`；教员成品 `code/L13/jizhang_完整版.py` 挪出仓库到本地 `code/测试/`。跟踪文件 **99 → 75**。
3. 新增 `docs/Python之后学什么_四条路与判据.md`（四条路 / 三层判定 / 时间落户 / 判据三问 / 三档必做清单 / 硬日历）。
4. `docs/灵感池.md` 立**收池规则**：**课程里的项目不进灵感池**（判据：课程安排的，还是我自己想做的？）→ L13「token 流水记录」出池，可复用参考数据搬进 L13 纪要 §7。

**2026-09-21（周一）放学格（学员开场"按协议开始"）**：**特别课 A/B 前置串讲 + 环境验证**（纪要 → `03_会话纪要\2026-09-21_特别课AB前置串讲与环境验证.md`）。
1. 两课讲义落库：`docs/特别课A_计算机通识_程序从哪来.md`、`docs/特别课B_网络与数据_从敲网址到看见页面.md`（因果链 + 动手清单 §6 + 费曼自检 §7；**答案不预给**）。
2. **学员完成 A 课动手作业**：`Measure-Command { python -c "pass" }` = **0.0849 s**（对照 L16 的 0.095 s）；`code\L13\__pycache__\jizhang.cpython-312.pyc` = 4908 字节；`where.exe python` → **WindowsApps 门牌（0 字节）**；相对路径双层实证 = `can't open file '...\code\L17\code\L17\sysin_demo.py'`。
3. **A 课第 1–2 层学员自证**：6 站顺序全对 → `a = a + 1` 数出**三次**动作 → 补上**跳转**（比较 + 条件跳转 = `if`/`while`）→ 自己去看了目录才答"直接运行**不**生成 `.pyc`"（**只有 `import` 的模块才落 `.pyc`**）。
4. **token 账口径澄清**（学员质询）：500+k = `cacheReadTokens`（累计重发量），5% = `pressureTokens/contextWindow`（53,761/1M）；**记忆占本次真实上下文 ≈ 20%，"3–5%"只在 14 会话 3.28 亿那个分母下成立**。纪律已进 `docs/教学机制与协作约定.md` 机制 4。
5. **环境真相改写**：`where.exe python` 给的是 **Windows 应用执行别名（0 字节门牌）**，真身在 `C:\Program Files\WindowsApps\PythonSoftwareFoundation.Python.3.12_3.12.2800.0_x64__qbz5n2kfra8p0`；**"只有一个 Python 3.12.10"仍成立**。→ 新硬纪律：**环境结论三步 = `where` → `Get-Item` → `Test-Path`**。
6. **教员侧账 +2 → 累计 16**：⑮ 拿平均数当本次事实；⑯ 把"门牌"当"房子"。
7. **学员记账**：定性代替定量 **2 → 3**（"CPU 能干的类别 → 一件"）；新强项 = 6 站全对 / 一次提示数出三次动作 / **先看目录再答**。

**2026-09-21（周一）放学格 2（学员："现在开始下一课"）**：**特别课 B 全程 + 数据落地/数据库**（纪要同一文件 §9）。
1. **学员起点**：问"按回车后那串文字最先送到哪"→ 答"**服务器吗？我完全不知道**"→ B 课从零搭因果链。
2. **B 课走完并复述通过**（学员自己说出 7 站，顺序全对）：域名 → DNS（名字→IP）→ 路由转发（**信使只看门牌不看内容**）→ 端口（**机器上很多程序，要知道跟哪个说话**）→ HTTP/URL（要什么 / 怎么问）→ **验证书再加密**（"加密"是第三步不是第一步）→ 返回结果。→ **判定：B 课主干「真正掌握」**。**教学武器：一个"送快递"类比贯到底**。
3. **数据落地 + 数据库**：内存→硬盘（进程一结束内存被回收）→ **序列化 = 有结构的内存 ↔ 只有文本的硬盘**；三层坑当场实验：**① `"a"` 追加破坏 JSON 结构**（`Extra data`）｜**② lost update：两个进程同时 `load→dump`，各自"成功"却静默丢一条**｜**③ 数据库对照实验：`sqlite3` 两个进程同时 `INSERT+commit` → `[初始, A, B]` 一条不丢**。
4. **学员自己的结论**：**"文件没法给记录安装身份证，会覆盖原来的记录。"**（→ 补：文件看不见"里面有几条"，只认整个文件；数据库知道每条是谁、还能编号。）
5. **BOM 坑学员亲历**：PowerShell `Set-Content -Encoding UTF8` 写出的文件**带 BOM** → `json.load` 报 `Unexpected UTF-8 BOM`。
6. **术语补齐**：缩写对照表（IPv4/IPv6、TCP、SQL、DNS、HTTP/S = 中文全名 + 在地图哪一站）；**并发 vs 并行**（并发=会切换，并行=有人手；**Python GIL → 多线程主要是并发**）。
7. **教员侧账 +2 → 累计 18**：⑰ **单条输出过载**（一股脑甩出 commit/原子性/主键 + SQL 动词，学员回"**你在说啥，我看不懂**"）；⑱ **PowerShell 引号转义想当然**（`\"` 被解析器吃掉 → 含引号脚本一律写 `.py` 文件再跑）。
8. **本条纪律由学员现场纠正**：**没喊收尾时不要批量读文件**（教员开场读了 9 个文件 ≈ 5.8 万 token）→ 已立 `[[no-read-storm-on-session-start]]`。

**2026-09-22（周二）放学格 3（军训 D13）**：**特别课 A 动手 + 客户端实操（真请求）**（纪要 → `03_会话纪要\2026-09-22_特别课A动手与客户端实操.md`）。
1. **学员亲手跑通**：`requests.get()` → `200` → `Content-Type` → `.json()` → **从三层嵌套里取出字段**，最终输出 `200 / text/plain; charset=utf-8 / <class 'dict'> / 4 / 31`。**这就是 S1 里 agent"会说话"的骨架**（换成 DeepSeek 地址照用）。
2. **三处卡点都是教学点**：① `KeyError: 'conetent-type'`（拼写少一字母 → **读报错最后一行，再去那一行找**）；② `except:` 把异常吞了（"出错了"三个字什么也没说）→ 改 `except Exception as e: print("出错了：", e)`；③ `data["temp"]` 是**猜的字段名** → 用 `type()/print(data)/len()` 探明结构后**自己念出路径** `data["current_condition"][0]["temp_C"]`（**零个新语法**）。
3. **接口观察**：`wttr.in` 的 `Content-Type` 自称 `text/plain`，实际给的是 JSON → **"自称 ≠ 事实"**；可靠做法是先试 `.json()`，拆不开就打印 `r.text` 看它到底给了什么。
4. **存储层物理账（实测 + 诚实标注）**：内存随机取值 **0.13 微秒**；走文件系统的"随机读" **4.1 微秒/次**（= 内存 **33 倍**）；顺序读 **11483 MB/s**、写 256 MB+`fsync` **2267 MB/s**——**本机内存 31.8 GB、文件只有 200 MB → 全被文件缓存接住，量到的是内存速度**。**硬盘真实随机读延迟本次没测到**（要 30+ GB 文件才绕得开缓存），业界量级（机械 5–10 ms / NVMe 50–100 µs）**明确标注为非实测**。四条因果：内存/硬盘差距 → O(n²) 的物理代价、**索引 = 少碰几次慢东西**、`.pyc` 为什么让第二次更快、**缓存无处不在所以性能难量**。
5. **一次真实的流程缺口（教员侧）**：学员连说两次"我完全不知道怎么写 / 我不了解流程和工具"——根因是**"两个程序、两个窗口、一个等一个"这种流程从未交代**，且教员把**未验证的外网地址**当作业布置。补齐流程与工具清单后顺利跑通。
6. **教员侧账 +2 → 累计 20**：⑲ 日期前后矛盾（开场写"今天仍是 09-21"，与自己写的"下一站 09/22"冲突，学员 `Get-Date` 纠正）；⑳ 布置未验证的作业（`wttr.in` 教员侧根本测不了）。
7. **学员新强项**：报错一次改对、**自己摸出三层嵌套路径**、主动追问 `Content-Type` 那行的含义（问在点上）。**老毛病复发 1 次**："不知道怎么写"未先说卡在哪一步（防滑三规第一条）→ 下次先写一句"卡在第几步、试过什么"。

**2026-09-22（周二）放学格 4（军训 D13 · 本格第二场）**：**特别课 B 收口 + 每日题 P1308**（纪要 → `03_会话纪要\2026-09-22_特别课B收口与每日题P1308.md`）。
1. **开场先拍板一笔**：学员带来 **AI Coding 创新应用大赛**（LearnBuddy，9 选题，要求部署到浏览器 + 源码仓库 + 历史对话）。我建议"并入 agent 主线、选方向二第 8 项"，但前提是截止宽松；学员核实 **2026/09/27 截止（仅 5 天，且 09/24 开学）** → **决定不参加**，直接"正常按进程开课"。**不新开第 10 条线。**
2. **学员当场质问"上次为什么跳步画草图、不让 B 课收口"** → 教员认账三条：① "按需"没给判据也没进待办 → 等于永不；② 用"B 课主干已掌握 / 本格已完成实操"这种表述把 **A 课的动手**说成了 B 课收口（连"7 个引导问题"都是记串的，grep 全仓库无此物）；③ **根因＝拿"我自己会了"当"你会了"**。→ 新判据：**收口 = 讲完 + 产物清空 + 证据**。
3. **特别课 B 收口完成**（§6 动手 4 项全做实 + §7 费曼 5 问全核销）：`Resolve-DnsName` 见到 **CNAME 绳子**（`www.baidu.com → www.a.shifen.com`）、`ping` 第一行印证"操作系统先做 DNS"、`curl -I` 见 `HTTP/1.1 200 OK` + `Content-Length: 0`、`curl -s` 撞出**管道解码坑**并修好（`[Console]::OutputEncoding = UTF8`）。
4. **B 课新教学点**：DNS 交回的是**一串记录 + 每段出处**（非"一个 IP"）；**一个名字对应一批地址**（同域名背后 ≥2 个中国移动广州 IP，两个 AS）；**两个 TTL 不是一回事**（DNS `TTL=900` = 缓存秒数；`ping TTL=52` = 还能走几跳）；**`Content-Type` 是自称、正文才是事实**（`-I` 只要头 → 光看头验不了）；**`<!--STATUS OK-->` 是百度注释不是 HTTP 状态**；**GET 参数进地址栏 → 留历史/缓存/日志/Referer，且能被 `<img src=...>` 骗着发出去**（口径：GET 看一眼、POST 改状态）。
5. **每日一题：P13224 弃 → P1308 AC**。P13224 判定**不适合当每日题**（评级普及−与已 AC 同档，但正解要"**根本不用模拟走路**"这一步跳跃）→ **单开一格当思维题课**；题面存档 `code\P13224_题目.md`。P1308 三版迭代（数词数 → 词长加和 → **指针扫描拿 `start` 当字符下标**）+ 补 `text.lower()` → **教员 13 用例全过**，学员已交洛谷（**提交结果未报**）。
6. **本格最值钱的一句**：**P1308 的两个官方样例都是"骗子样例"**——第一版有明确位置口径错也照样过（第一个 `to` 恰在下标 0）。**学员从"样例过"推到了"用例过"。**
7. **教员侧账 +7 → 累计 27**：㉑ **跳步、B 课未收口就提议画草图**；㉒–㉖ **同格自犯 5 次数错**（`is a to be` 说 6 实为 5；`is a big to be` 说 8 实为 9；"末位加 to"位置说 24 实为 0；`be or not to…` 说 6 实为 10、又说 32 实为 30）——**全部是"想出来的数"，学员复算抓出 3 次**；㉗ **穷举脚本没设规模约束**（3×3 全盘对拍爆两次，一次 120 秒超时、一次学员手动按停）。
8. **纪律新增**：① **教员自述里的数字与"查证学员的数字"同等对待**——凡位置/下标/长度/个数/耗时/等级，**先跑脚本贴输出，跑不了就不说这个数**；② **收口 = 讲完 + 产物清空 + 证据**，讲义里"课上核对/口头复述"的项，收尾必须核销并**写进待办**；③ **布置穷举/验证脚本前先算规模并设预算**。
8. **P1308 解法二（正则）学员自己写成并入库**：`code\洛谷\P1308(2).py` —— **13 行**（原手写扫描版 23 行），`solve(target,text)` 函数形状 + `__main__`；教员 16 用例全过（含原版唯一挂掉的**制表符分隔**），10⁶ 规模 **122 ms → 21 ms**。**新工具 `re` 首次实战**（`\b` 词边界 / `IGNORECASE` / `search` / `findall`）。**新硬结论：`input()` 不把 `\n` 留在字符串里，`sys.stdin.readline()` 会**（教员先给错的建议，学员问出后现场改正）。
9. **学员定：题目/样例只留本机、不进 GitHub（2026-09-22）** —— 原话："**是指不上传到 GitHub，如果方便你调用可以放文件夹里**"。做法：`code\洛谷\题目\`（**已 gitignore**，同 `code\测试\` 的模式）放题面 `<题号>.md` + 样例 `<题号>_in/out<n>.txt`；**新增 `code\洛谷\拉题.py`**（`python code\洛谷\拉题.py P1308 P13224`，解析洛谷页面内嵌 JSON 块，一次拉题面+全部样例，可复用）。**入库的只有解法**（`code\洛谷\P1308(1).py` / `(2).py`）。→ 教员原落后又删除的 `P1308_题目.md` / `P13224_题目.md` 与样例，**已按新结构重建在本地忽略目录**。
10. **学员对教员定位的反馈（不美化）**：学员说"是不是协议限制太多/能力欠缺？我会优先用网页版 DeepSeek 查 bug，实在解决不了才问你"。**两点都成立**：不代写协议天然更慢（它防的是能力错觉）、算术复算确有缺陷。教员提的边界（**造用例/对拍/边界/复杂度估算/跨会话台账**是网页版没有的角色）**学员未表态**。→ **学员当场要求：以后每日算法题给"优化空间/新工具学习"的建议**（教员已给三档建议，见纪要 §8；**学员尚未表态采纳哪条**）。

**2026-09-23（周三 · 军训最后一天）**：**五周冲刺计划定型 + 学员独立 review 计划 + P1086 用例级验收**（产物：`docs/agent_v1_五周冲刺计划.md`）
1. **学员带来一份"Pi 技术栈"文档问可行性**。教员核实后给结论：**架构方向可行，但那份文档有一处会把人带偏的 P0 错误**——它称"V4.1 Flash 不用 OpenAI 标准 `tool_calls`、必须手写 DSML 解析"，**实为自托管（vLLM）问题**；官方 API 返回标准 `tool_calls`（本机 dsh 的 `dsh-llm-deepseek` 适配器源码里也没有一行 DSML 解析）。**真正的 API 级坑它没写**：带 `tools` 时历史所有轮次的 `reasoning_content` 必须全程回传，否则 **400**。
2. **学员两次更正教员的口径**（都成立）：① 项目目的是**"学习理解 + 简历优势"**，不是"有个工具能用"→ 目标函数从"工具可用性"改为**"理解深度 + 可展示证据"**，行数只是副产品；② **否定"每格只能学一个新概念"**——那条约束的是**教员输出方式**，被教员挪用成了约束**学员速度**（见纪律新增）。
3. **五周冲刺定型**：窗口 **09/24–10/28**，**每格 1.5–2 小时**，15 格（最低可行 12 格）；三个检查点（W1 末 / W2 末 = 生死线 / W3 末）；砍掉清单按"**学习密度低**"判据（TUI/沙箱/RPC/DSML 解析器/MCP 适配器）。施工图 §5 固化四类故障的**主动造法**（不许等它自然发生）。
4. **P1086 用例级验收**：官方样例 37 ✓、自造边界 5 条全过 ✓、**300 组随机 0 不一致** ✓ → 入库 `code/洛谷/P1086.py`（26 行），已推送（commit `e044153` / `0011a36`）。**P1308 学员报 AC**（提交结果落库）。
5. **学员对计划做了一次完整 review（new 强项）**：自己找出 7 条问题，其中 **6 条成立**——日历冲突（W1 检查点要流式，但流式排在第 6 格）、**12 格版没写删什么**、**thinking 默认关却要复现"`reasoning_content` 漏传 400"**（自相矛盾）、**压缩只追加摘要不改请求体 ≠ 压缩**、**1M 上下文按公式要到 98.4 万 token 才触发**（练习根本跑不到）、**故障复现不能靠偶然**（应主动 `gc.collect()` / 造假 429）、**快照停在 09-22 与计划不同步**。→ **全部采纳并落进施工图**。第 7 条（"流式输出会随工具调用一起实现、不是额外工作量"）成立但只在流式路径上，非流式路径确实没有——**已明确流式降为选做**。
6. **教员侧账 +7 → 累计 34（㉘–㉞）**，全部发生在核验 P1086 的过程里：**没读那 28 行代码就先连写 6 个对拍脚本**，其间参考解写错 2 次、手算错 4 次，最严重的是**拿旧版本文件测出的结论当学员当前文件的结论**（**没先 `Get-Item` 看修改时间**）。学员一句话点破：**"你检查我的代码不就行了吗？为什么要自己写这么多还一直出错"**——**成立**。

## 课程进度（对照根 README 日历）


| # | 日期 | 内容 | 状态 |
|---|---|---|---|
| L01–L16 | 09-06 ~ 09-19 | 见纪要索引 | ☑️ 全完成 |
| L17 | **09-20 提前** | 总复习 +《我的 Python 路线图》（产出 `docs/我的python路线图.md`） | ☑️ **完成 — 主线收官** |
| 特别课 A/B | 09-21–09-22 | 计算机通识：A=CPU/内存/硬盘/程序怎么跑 + 命令行；B=IP/HTTP/服务器/数据库 → **agent S0 前置** | ☑️ **全收口**（09-21 串讲 + A 课动手 + B 课 §6 动手 4 项 + §7 费曼 5 问，**09-22 核销**）；剩 **SQL 段** 挪进「下一步」，**TCP 细节 / 服务端框架 = 按需** |
| **agent v1 五周冲刺** | **09-24 ~ 10-28** | **W1–W5，每格 1.5–2 小时**；施工图 `docs/agent_v1_五周冲刺计划.md` | ⏳ **未开跑**（W1 第 1 格 = 09/24；**计时基准从 W1 开跑算**） |

洛谷战绩：**17×AC**（09-22 **P1308** 已确认 AC；09-23 新增 **P1086 花生采摘**，300 组随机用例级验收 0 不一致）。

## 下一步
1. **09/24 开学 → agent v1 W1 第 1 格**（`async`/`await`/事件循环 + `httpx`；第 2 格起碰 `gc.collect()` 主动触发任务回收）。**开场第一件事：核对本文快照时间与真实日期，确认计时基准。**
2. **W1 末（09/30）数一次真账**：实际用了几格/每格多久/卡点在哪 → 用它替换所有进度推算（**编制计划时不做第二次估算**）。
3. 开学后并行：C 语言课（**当天作业当天清**）+ 每日 1 题洛谷（**用例级验收**）+ 蓝桥杯报名（10–11 月）。
4. 悬着：**P13224 思维题课**（教员公式尚未小规模对拍验证完，别开课）；SQL 段（`sqlite3` 已用过，补"表/行/列/主键"）。

## 触发器状态
- ✅ **已关闭（09-20）**：`sys.stdin`（未知行数读到 EOF，`code/L17/sysin_demo.py` 验收 `3 6`）、`pip`（装上 **requests 2.34.2**，`code/L17/hello_requests.py` 跑通）。
- ✅ **09-20 转正三项**：**切片** `s[起:止:步]`（含 `[::-1]` 倒序；学员跑通并说"会了"）→ 已进 `docs/语法模板卡.md` 正区（§2 示例 + §8 对照表），并新增 §11「迭代输入流与装第三方库」。
- ⏳ 仍未解锁：`d.keys()/values()`、set 交并运算（触发器到了再解锁）。

## 纪律新增（09-20 / 09-21）
- **核验学员代码：第一步永远是"读代码 + `Get-Item` 看修改时间"（2026-09-23 立，学员点名）**：工具只用来**确认**读完的结论，不用来**找**结论；**我的脚本输出 ≠ 事实**（与"登记≠存在、门牌≠房子"同族）。**Why:** 09-23 核验 P1086 时教员没读那 28 行就先写 6 个脚本，参考解写错 2 次、手算错 4 次，还用**旧版本文件**的测试结果宣布学员"有 bug"——学员原话"**你检查我的代码不就行了吗？为什么要自己写这么多还一直出错**"。**How to apply:** 核验前先 `Get-Item <文件> | Select LastWriteTime`，再通读代码；对拍只在"读完仍不确定"时上，且**尺子与参考解互相矛盾时必须先停下来**。
- **"一条消息只推一个概念"约束的是教员输出，不是学员速度（2026-09-23 学员否定后澄清）**：**不设"每格只能学一个新概念"这类额度**——哪个概念挡在交付物前面就学到够用。**Why:** 教员把学员 09-21 立的规则（管教员的嘴）挪用成了管学员的手速，等于给自己一块"额度用完"的挡箭牌。**How to apply:** 引用学员以前立的规则前，先核**那条规则当初约束的是谁**；每格开头可以直接认领"这格要碰 N 个新概念"。
- **提交节奏（学员要求）**：**git 提交只在"确认收尾"时做一次**；会话过程中只写盘。权威全文 → `docs/教学机制与协作约定.md` 机制 4。
- **数量纪律**：问到"多少个 / 多久 / 几倍"，答案里**必须出现一个数**（没有数 = 没答完）。
- **token 卫生（09-21）**：① **工具输出瘦身**（不整页拉网页、命令只打关键行、文件只读相关区间、同一文件不重复整读）；② **输出瘦身**（长解释进讲义文件，对话只给"结论 + 下一步"）；③ **一格一存档、开新会话**（最大杠杆）。→ 机制 4 有全文与实测依据。
- **灵感池收池规则（09-21）**：**课程里的项目不进灵感池**。
- **一条消息只推一个概念（09-21 学员"你在说啥，我看不懂"后立）**：新概念**不上术语**；讲完一个小结论**就停下来问一句**；连续提问 ≤2 问、连续新概念 ≤1 个。同源于"一轮一件事"与"先给地图再问问题"。
- **没喊收尾就不批量读文件（09-21 学员要求）**：开场只读 `MEMORY.md`；不读 `SKILL.md`；查旧内容只读相关区间。→ EnGramory `[[no-read-storm-on-session-start]]`。
- **布置作业前先验证材料与流程（09-22 立）**：布置动手作业前必须确认两件事——① **材料/地址是否可用**（教员自己测不了的外网地址**不许当作业**，要给就必须标注"我没验证过"）；② **这个流程学员是否做过**（"两个程序、两个窗口、一个等一个"这类全新流程，**必须先交代流程再布置**）。
- **术语必须给中文全名（09-21）**：英文缩写首次出现时，同时给**中文全名 + 它在地图哪一站**（学员主动问"你说的专业术语都是什么意思"）。
- **教员自述里的数字 = 待验证断言（09-22 立，学员三次复算抓出三次）**：凡**位置/下标/长度/个数/耗时/难度等级/账目计数**——**一律先跑脚本、贴输出；跑不了就不说这个数**。**Why:** 09-22 第二场教员自犯 5 次数错，全部发生在"教学叙述的顺带句"里（不是被问的那个数）；学员侧数字反而会去核。**How to apply:** 报数前自问"这是刚跑出来的，还是顺手写的"；顺手写的 → 立刻跑或删掉这个数。
- **收口 = 讲完 + 产物清空 + 证据（09-22 立）**：讲义里凡写"课上核对 / 课上口头复述"的项，收尾必须核销；**欠的项必须写进待办**——不写进待办就等于没有。**Why:** B 课 §6/§7 挂了整整一天，因为"按需"没判据、也没进待办。
- **穷举/验证脚本先算规模并设预算（09-22 立）**：布置对拍或穷举前，先估算操作量并设时长上限；**从小到大递进**（先 1×1/2×2，再上 3×3）。
- **每日算法题改为"用例级"验收（09-22 立）**：交洛谷前必须有**官方样例 + 自造 3 条边界用例**，并逐条跑过；官方样例从 `code\洛谷\题目\<题号>_in/out*.txt` 本地读（该目录 gitignore）。**Why:** P1308 第一版有明确位置口径错，**两个官方样例照样全过**（第一个 `to` 恰在下标 0）——**样例是骗子样例**。
- **自造边界的"三句固定问"（09-23 由 P1086 补）**：官方样例 + 自造边界全过**仍可能大面积挂**——P1086 的卡点是"**摘到一半发现下一棵去不了**"这种**中途失效**，而自造边界全在"单棵/首棵"上打转。→ 每次自造边界固定问：① 最小规模？② K 取 0 和取极大？③ **"中途某一棵不行"和"最后一棵不行"是不是同一件事？**
- **故障复现要"主动造"，不许等它自然发生（09-23 立，学员 review 提出）**：`gc.collect()` 主动触发任务回收；429 用**假响应**注入而不是等真实限流；复现 `reasoning_content` 400 必须**先开 thinking**；压缩要用**可配置的小窗口**（1M 上下文按公式要跑到 98.4 万 token 才触发，练习根本到不了）。

## 周体检（每周日 10 分钟 · 新作息里唯一的固定仪式）
| 周次 | 本周完成 | 最扎实 / 最虚 | 欠什么 | 下周第一件事 |
|---|---|---|---|---|
| 2026-09-20（首次） | L13 收尾 → L15 Git → L14 README 重构 → L16 复杂度三题 → L17 总复习+路线图 | 扎实 = **L16 复杂度**；虚 = **L13 台账项目** | P5711 入库（**本次已补**）、切片 / `keys()/values()` 未解锁、L13 待加固 | **特别课 A**（09/22），09/24 开学进 agent S0 |

## 待办（当前有效）
1. **陈旧 py 启动器注册**：`HKCU:\Software\Python\PythonCore\3.11 → D:\app\Python\`（**目录已不存在**）→ `py -0p` / `py -3.11` 会误导。可选清理 `Remove-Item 'HKCU:\Software\Python\PythonCore\3.11' -Recurse`。
2. **本机只有一个 Python**：Store 版 **3.12.10**（`python` 指向它）；`requests` 已装进它。装库铁律：**`python -m pip install 包名`**。
3. 可选：全局 `user.email` 由真邮箱换成 noreply（仓库级已是 noreply）。
4. ~~`P5711` 代码仍未入库~~ → **已补入库（2026-09-20）**：`code/L02/p5711.py`（从 `code/L02/my_day2.py` 第 29–34 行原样抽出，学员授权补入库）。
5. **09/24 开学后**：12 月中主线语言复查；高数跟课走（军训期不自学）。

## 学员易错点（09-20 更新）- 口径/初始状态 **10**（新增两处：① 验收时输入多一个空行 → `int('\n')` ValueError；② 相对路径起点搞错 → 人在 `code\L17` 里又写 `code\L17\...`，路径拼成两层）｜状态同步 3｜谁依赖谁·值从哪来 3｜重写丢约束 3｜产物未落库 3｜引用 vs 调用 2｜暂存区概念漏 1｜提交信息不具体 2。
- L16 遗留三种：换汤不换药 1｜读取缺省值漏 1｜**定性代替定量 2 → 3**（09-20 一次；**09-21 又一次：问"CPU 能干的类别"答"一件"**）。
- **09-20 新增一种**：**答非所问 1**（问"两种写法的差别"，答成"平方增长怎么来的"）。
- **09-21（放学格）更新**：**答非所问 +1 → 2**（问"两台机器怎么互相听懂"，答成"URL 是什么"）；**定性代替定量 +1 → 3**（"CPU 能干的类别 → 一件"）。**但两次被点后都当场改对**，且第二次自己接上了端口→程序的因果。
- **09-22 更新**：**口径/初始状态 10 → 11**（`Content-Type` 拼成 `Conetent-Type` → `KeyError`；**读报错最后一行就能解决**）；**新技能养成 = 报错被点破后一次改对、自己念出三层嵌套路径**。
- **09-22 第二场更新**：**口径/初始状态 11 → 12**（`words` 里装的是**词**不是**字符**——"我手里这个容器装的是什么"又一次成为病灶，同族于"改了东西 ≠ 进了该去的地方"）；**大小写口径（新）**：只 `lower()` 了关键词、没 `lower()` 文章 → 3 个大小写用例全挂，而**官方样例仍全过**。
- 规律不变：**"改了东西"≠"它进了该去的地方"**；**问到"多少个/多久/几倍"必须出现一个数**。

## 学员强项（09-20 新增 / 09-21 补 / 09-22 补 / 09-23 补）
- **09-23（对计划做完整 review，新层级）**：拿到 `docs/agent_v1_五周冲刺计划.md` 后**逐条挑错**，7 条里 6 条成立，且全是**真漏洞**不是措辞问题：日历自相矛盾（W1 检查点要流式、流式却排在第 6 格）｜**"12 格最低可行版"没写删什么**｜**默认关 thinking 与"复现 reasoning_content 400"自相矛盾**｜**"追加摘要"≠ 真正压缩（请求体没变小）**｜**1M 上下文按公式要到 98.4 万 token 才触发，练习跑不到**｜**故障复现不能靠偶然，要主动触发**｜**进度快照停在 09-22，与计划不同步**。→ 教员**全部采纳**。**这是"审核别人的设计"这一层的第一次**（此前只会审自己的代码/用例）。
- **09-22（客户端实操）**：**自己摸出三层嵌套路径**（`type()/print(data)/len()` 探路法，一次学会）；**报错被点破后一次改对**（拼写、`except` 吞异常两处）；主动追问 `Content-Type` 那行的含义——**问在了真正的点上**（"自称 ≠ 事实"）。
- **09-22（P1308 第二场）**：① **自己造用例把代码打挂**——三版迭代全靠自己改，最终教员 13 用例全过；② **从"样例过"推到"用例过"**（他自己说"我写完后的代码成功跑通了"，教员复测才补上大小写那一刀）；③ **原文优先、最后才 `lower()`**（`text` 全程保持原文，位置信息不被破坏）——"位置类题目"的正确姿势；④ **从"数词长拼算式"跳到"指针扫、拿 `start` 当字符下标"**，不再拼凑算式。
- **09-21（特别课 A 前置）**：**6 站因果链凭记忆顺序全对**（源码→加载器→编译→解释→CPU+内存→屏幕）；`a = a + 1` 经**一次提示**就数出三次动作（取出/加/写回）；补上「跳转」后自己推出 `if`/`while` 的机器层实现；最后一问**先去看目录再回答**"直接运行不生成 `.pyc`"（**"看了再说"**）。
- **能凭记忆画出结构**：第一版覆盖约六到七成，且**主动把"控制流"独立成块、"文件与持久化"另起一块**；被指出 4 处概念错后**第二版一次全改对**，还自己补上 `append/break/continue/sum/in/sorted/type/bool`。
- 主动追问机制（`sys.stdin` 是什么、`.stdin` 有什么用、调试是不是只能在 PowerShell）；报错连代码一起给、按指点自己改。

## 教员侧账（累计 27 次；09-22 两场共新增 7 次）
- ㉗ **穷举脚本没设规模约束（09-22 第二场）**：为验证 P13224 的解法写了"3×3 全棋盘对拍"，**爆了两次**（一次 120 秒超时、一次学员手动按停）。→ **纪律：布置穷举/验证脚本前先算规模并设预算**（先 1×1/2×2 几百张，过了再上 3×3）。
- ㉒–㉖ **同一格自犯 5 次数错（09-22 第二场）**：`is a to be` 的位置说成 6（实为 **5**）；`is a big to be` 说成 8（实为 **9**）；"样例末位加 to"的位置说成 24（实为 **0**）；`be or not to be…to` 的第一个 `to` 说成 6（实为 **10**）、"最后一次"说成 32（实为 **30**）。**全部发生在教学叙述的顺带句里，学员复算抓出 3 次。** → **纪律：教员自述里的数字与"查证学员的数字"同等对待——先跑脚本贴输出，跑不了就不说这个数。**
- ㉑ **跳步、B 课未收口就提议画草图（09-22 第二场）**：学员直接质问"上次为什么没让我收口特别课 B"。根因＝**拿"我自己会了"当"你会了"**。→ **纪律：收口 = 讲完 + 产物清空 + 证据**；讲义里"课上核对/口头复述"的项，收尾必须核销并写进待办。
- ⑳ **布置未验证的作业（09-22）**：把 `wttr.in` 这个**外网地址**当作业给学员，教员自己那侧 5 个站点全不通、**根本没验**；叠加**流程缺口**（两窗口协作从未交代）→ 学员连说两次"我完全不知道怎么写 / 我不了解流程和工具"。→ **纪律：布置动手作业前先验材料 + 先确认流程学员做过**（见「纪律新增」）。
- ⑲ **日期前后矛盾（09-22）**：本格开场写"今天仍是 09-21 那一格"，与自己落盘的"下一站 09/22–09/23"直接冲突；学员 `Get-Date`（12:27 周二）纠正。→ **纪律：涉及日期先 `Get-Date`，且与已落盘的"下一步"对照**（09-09、09-17→09-18 之后**第三次**同类）。
- ⑰ **单条输出过载（09-21 放学格 2）**：讲完数据库后，一条消息里同时甩出"文件/数据库对照表 + `commit`/原子性/主键编号 + 三个 SQL 动词 + 下一格预告"→ 学员回"**你在说啥，我看不懂**"，被迫整段重讲。→ **纪律：一条消息只推一个概念；新概念不上术语；讲完一个小结论就停下来问一句**。与 09-17"一轮一件事"、09-19"先给地图再问问题"**同源，是同一个毛病的第三次复发**。
- ⑱ **PowerShell 引号转义想当然（09-21 放学格 2）**：在 `pwsh -Command` 里用 `\"` 转义双引号 → 解析器报 `Missing argument in parameter list`。→ **含引号/多行的脚本一律先写成 `.py` 文件再跑**。
- ⑯ **把"门牌"当"房子"（09-21）**：断言 `where.exe python` 是"照妖镜"、足以定位程序本体；实际它给的 `...\WindowsApps\python.exe` 是 **Windows 应用执行别名 = 0 字节门牌**，真身在 `C:\Program Files\WindowsApps\PythonSoftwareFoundation.Python.3.12_3.12.2800.0_x64__qbz5n2kfra8p0`。→ **新硬纪律：环境结论三步 = `where`（有几个）→ `Get-Item`（多大/是不是门牌）→ `Test-Path`（真身在哪）**，少一步就是"想出来的"。
- ⑮ **拿平均数当本次事实（09-21）**：把"14 会话平均 194K/次、记忆占 3–5%"当成本次开场的预期，被学员追问（开场 cacheRead ≈ 51.6 万、真实上下文 32.2K/记忆占 ≈20%）。→ **纪律：引用任何用量数字先报口径与分母，要"本次"的数就现场读 `~/.dsh/storages/session_projcache/sessions/<id>.json`**。
- ⑭ **把陈旧注册当事实**（09-20）：用 `py -0p` 的输出断言"机器上有两个 Python（3.12 + `D:\app\Python\3.11`）"，**没做 `Test-Path` 就下结论**；实际 `D:\app\Python\` 不存在，那是 py 启动器注册表里的**陈旧登记**，学员当场纠正。（**与 ⑯ 同族：登记 ≠ 存在；门牌 ≠ 房子。**）
- 09-19 的 ⑬ 尺子用错单位、以及"收尾只写盘未提交"的流程问题，见前一日条目。

## 会议纪要索引
- 早期：`2026-0906-0908_补记.md`｜`…L04…`｜`…L05…`｜`…L06字典与P1598.md`｜`2026-09-10_阶段收尾与纪律定型.md`｜`2026-09-10_诊断复习与刷题四步法.md`｜`2026-09-11_P1047与重合记法.md`｜`2026-09-12_L07函数def.md`
- 近期：`2026-09-13_L08收官考勤打卡.md`｜`2026-09-13_记忆系统改造与L08复修.md`｜`2026-09-13_L09刷题方法课与两道OJ.md`｜`2026-09-14_L10文件读写与存档版.md`｜`2026-09-15_L11面向对象class.md`｜`2026-09-17_L12模块与标准库.md`｜`2026-09-18_L13token台账.md`｜`2026-09-18_L15git入门与推github.md`｜`2026-09-18_L14机动-README重构.md`｜`2026-09-19_L16算法热身与复杂度.md`｜`2026-09-20_L17总复习与路线图.md`｜`2026-09-21_维护格_token审计与仓库瘦身.md`｜`2026-09-21_特别课AB前置串讲与环境验证.md`｜`2026-09-22_特别课A动手与客户端实操.md`｜`2026-09-22_特别课B收口与每日题P1308.md`｜**`2026-09-23_五周冲刺计划与P1086验收.md`**
